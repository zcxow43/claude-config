# Backend Rules

## Scope

What belongs in `specs/backend/`: REST APIs, controllers, services, mappers, DTOs, validation, business logic, transactions.

## Standing Rules

(none yet — add project-specific backend spec rules here as they come up, following the shape of `frontend.md`/`dba.md`'s Standing Rules sections)
